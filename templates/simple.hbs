<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.replit.com/agent/bootstrap-agent-dark-theme.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/static/css/main.css" rel="stylesheet">
    <link href="/static/css/custom.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <i class="bi bi-broadcast me-2 fs-4"></i>
                <span class="fw-bold">Pod Force</span>
            </a>
        </div>
    </nav>

    <main class="main-content pt-5">
        <div class="container py-4">
            <h1>Pod Force</h1>
            <p>{{episodes.length}} episodes available</p>
            
            {{#if episodes}}
            <div class="row g-4">
                {{#each episodes}}
                <div class="col-12">
                    <div class="card episode-card h-100 border-0 shadow-sm">
                        <div class="card-body d-flex">
                            <div class="episode-image me-3">
                                <img src="{{image}}" alt="{{title}}" class="rounded">
                            </div>
                            <div class="flex-grow-1 d-flex flex-column">
                                <div class="episode-header mb-2">
                                    <span class="badge bg-primary me-2">{{feedName}}</span>
                                </div>
                                <h5 class="card-title mb-2">
                                    <a href="/episode/{{guid}}" class="text-decoration-none">
                                        {{title}}
                                    </a>
                                </h5>
                                <div class="mt-auto">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="text-muted small">
                                            {{published}}
                                        </div>
                                        <a href="/episode/{{guid}}" class="btn btn-primary btn-sm">
                                            <i class="bi bi-play-fill me-1"></i>
                                            Listen Now
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
            {{else}}
            <div class="text-center py-5">
                <h3 class="text-muted mt-3">No episodes found</h3>
                <p class="text-muted">Check your feed configuration or try refreshing.</p>
            </div>
            {{/if}}
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/static/js/main.js"></script>
</body>
</html>